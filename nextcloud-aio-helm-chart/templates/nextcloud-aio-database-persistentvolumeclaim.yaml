apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: nextcloud-aio-database
  name: nextcloud-aio-database
  namespace: "{{ .Values.NAMESPACE }}"
spec:
  {{- if .Values.STORAGE_CLASS }}
  storageClassName: {{ .Values.STORAGE_CLASS }}
  {{- if .Values.STORAGE_RECLAIM_POLICY }}
  persistentVolumeReclaimPolicy: {{ .Values.STORAGE_RECLAIM_POLICY }}
  {{- end }}
  {{- if .Values.STORAGE_LOCAL_BASE_PATH }}
  local:
    path: {{ .Values.STORAGE_LOCAL_BASE_PATH }}/nextcloud-aio-database
  {{- end }}
  {{- end }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.DATABASE_STORAGE_SIZE }}
